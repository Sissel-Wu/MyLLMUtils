[tool.poetry]
name = "myllmutils"
version = "0.2.0"
description = "A lightweight library to ease LLM use"
authors = ["Sissel-Wu <sissel126@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
openai = "^1.51.2"
tiktoken = "^0.7.0"
httpx = { version = "0.25.2", extras = ["socks"] }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
